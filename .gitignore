# Build artifacts
/target

# Cargo
**/*.rs.bk

# Development scratch — not part of the public source tree
.dev/
# Same private working area, other spellings — the `.dev/` rule above only
# matches the directory, so scratch trackers and audit dumps beside it were
# being tracked by accident.
.dev-audit/
.dev-*.md
.tmp/
.claude/

# Editor / OS
.DS_Store
*.swp
*.swo
.vscode/
.idea/

# v2.0 CV-infra — luna-fuzz nested target/ (workspace-excluded crate)
crates/luna-fuzz/target/
crates/luna-fuzz/Cargo.lock
fuzz/Cargo.lock
fuzz/corpus/
fuzz/artifacts/
fuzz/target/

# Python bytecode from the web/ tooling
__pycache__/
*.pyc
